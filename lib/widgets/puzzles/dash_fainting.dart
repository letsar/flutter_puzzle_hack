import 'package:flutter/widgets.dart';

class DashFaintingPuzzle extends StatelessWidget {
  const DashFaintingPuzzle({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/dash_fainting.gif');
  }
}
